# KS X 3267 통합제어기 — 자가시험 절차 (SPS-X KOAT-0004-7466 §5.4/§5.5 + KOAT 검정기준 116/117 통합제어기)

작성 2026-08-30. 두 문서를 우리 제어기에 그대로 대응시킨 것. 자동화 가능한 단계는 스크립트가, 화면·장시간·전원 항목은 수동 증적표로.

## 0. 역할 (시험 문서 용어 ↔ 우리 것)

| 문서 용어 | 우리 것 |
|---|---|
| 시험대상장비 (제어 가능형 제어기, 표 4) | RPi + Node-RED + `ks3267d` 드라이버 + 웹/키오스크 화면 (`스마트그린 통합제어기`) |
| 시험장비 (센서/구동기 노드 흉내) | `tools/ks3267-sim` — `--units 1:actuator,2:sensor --ctl 5030` (시험장비 제어 API로 관측치·상태를 가상 변경, 명령 수신 확인) |
| 제어기의 인터페이스(화면) | 제어판(웹/모바일/키오스크) → 로컬 제어 REST `POST /api/control/local` → `execute_control` → 표준 명령 조립 → 드라이버 FC16 |
| 노드정보 1~8 / 디바이스 코드 101~ | 드라이버 `discover()` (`GET /discover?unit=`) |

## 1. 시험 환경 기동 (RPi 1호, 루프백 TCP — HW 오면 `--port /dev/smartfarm-485-std` 로 동일 절차)

```bash
cd ~/smartfarm/ks3267
pm2 start ks3267-sim/sim.py --name ks3267-sim --interpreter ./venv/bin/python -- --tcp 5020 --units 1:actuator,2:sensor --ctl 5030 --log-frames
pm2 start ks3267d/ks3267d.py   --name ks3267d   --interpreter ./venv/bin/python -- --tcp 127.0.0.1:5020 --units 1,2 --nr-url http://127.0.0.1:1880/api/ks3267/status
./venv/bin/python ks3267d/selftest_sps7466.py --out ~/smartfarm/ks3267/evidence
```
결과: `evidence/<시각>/report.md`(항목별 ✅/❌), `results.json`, `frames.txt`(TX/RX hex — 시험기관 제출 증적).

## 2. SPS-7466 표 4 「제어 가능형 제어기」 시나리오 ↔ 자동/수동

| No | 시험 | 문서 단계 | 우리 절차 | 자동화 |
|---|---|---|---|---|
| 1 | 5.4.1 연결시험 | RS485 연결, 9600, 슬레이브 ID 설정 | 드라이버 `/conntest` = 화면 「§5.4.1 연결 시험」과 같은 a)~d) 판정(센서·구동기 노드 둘 다 d) 응답) + **당일 준비 점검**(표준 FTDI 포트 인식, 시뮬레이터 중에도 9600 8N1 로 열리는지 실제로 열어 봄). 시뮬레이터 환경에선 a)b) 를 루프백·시뮬레이터 설정이 대신한다고 단계 이름에 적는다 (2026-09-15) | ✅ `t_541` |
| 1 | 5.4.2 센서 노드 검색 a)~d) | a) 5.4.1 뒤, b) 시험장비에 연결 센서 개수·종류 설정, c) 디폴트맵 센서 노드 인지, d) 개수·종류 일치 | a) `/conntest` d) 노드 응답 → b) 시험장비 `POST /spec {attached:[1,2,3,4,13]}` 로 **스펙을 실제로 바꿈**(온도 3·습도 1·CO2 1) → c) `/discover?unit=2` 노드정보 1~6 디폴트값 → d) 제어기 인식 = 설정(개수·종류·순번) → 전체 30개로 되돌려 다시 d'). 화면 「연결된 디바이스」 머리에 개수·종류 요약(`deviceKindSummary`)이 떠서 시험장비 설정과 바로 대조 (2026-09-15) | ✅ `t_542` |
| 1 | 5.4.3 데이터 확인 a)~d) | a)b) 관측치 가상 변경 → 제어기가 읽는가; c)d) 상태가 **일정주기마다** 변경 → 매번 읽는가 | a)b) `/sensor {value}` 21.5/30.25/−3.0/28.8 → `/status` 반영. c) 시험장비 `POST /sensor/cycle {statuses:[103,102,0], period:4}` 로 4초마다 순환 → d) 드라이버 **변화 이력** `/changes` 에 103→102→0→103 이 순서대로, 간격 4.0s 로 남음. 화면: 센서 노드 카드 「§5.4.3 데이터 확인 — 관측 변화 이력」(센서별 값·상태 변화 횟수, 상태 변화 주기, 시각별 표). 9/4 판은 상태를 한 번씩만 바꿔 '일정주기' 가 없었다 (2026-09-15) | ✅ `t_543` |
| 1 | 5.4.4 데이터 저장 (10분↑) a)~d) | a) 저장 기능 있음(저장주기 1분), b) 5.4.3 를 10분 이상, c) 관측치·d) 상태가 저장주기대로 | **표준 센서 1분 스냅샷** `ks_sensor_status`(2026-09-15 신설): NR 「표준 구동기 1분 스냅샷」이 탐색된 표준 센서 **전부**의 관측치·상태를 `sensorRows` 로 같은 요청에 실어 `POST /internal/actuator-status` → 서버 표. 운영 `sensor_data` 와 달리 상태 101~103 이어도 생략하지 않는다. 조회 `GET /api/sensor-status/:farmId`(+`/export`, `/sensors`), 화면: 표준노드 탭 센서 카드 「§5.4.4 데이터 저장 확인」(센서별 행 수·기대·빈틈·상태 종류·판정) + 보고서 › 데이터 조회·추출 「표준 센서 관측치·상태」. 자가시험 `--only 5.4.4 [--duration 600]`: 값 5종·상태 [0,0,103,0,102,0] 을 60초 순환 10분 → 서버 되읽기(`/internal/sensor-status`, 농장 키, **User-Agent 필수 — 기본값은 Cloudflare 403**) → 1분 간격·값 있음·상태 103/102 포함 판정. **오프라인 사본**: 드라이버 `localstore.py` 가 매분 `state/snapshots.db`(SQLite, 60일)에 센서·구동기 행을 남기고 `GET /local/sensor-status|actuator-status|summary`(csv 가능) → 키오스크 같은 출처 `/api/ks3267-comm/local-*` → 카드 「제어기 로컬 저장」 | ✅ `t_544` (NR 함수 교체·백엔드 배포 완료 9/15) |
| 2 | 5.5.1 구동기 노드 검색 a)~d) | a) 5.4.1 뒤, b) 시험장비에 연결 구동기 개수·종류 설정, c) 디폴트맵 구동기 노드 인지, d) 개수·종류 일치 | a) `/conntest` d) 노드 응답 → b) 시험장비 `POST /spec {attached:[1,2,3,17,18]}` 로 **스펙을 실제로 바꿈**(스위치 3·개폐기 2) → c) `/discover?unit=1` 노드정보 1~6 디폴트값(제품타입 2·채널 24) → d) 제어기 인식 = 설정 → 전체 24개로 되돌려 d'). 화면 「연결된 디바이스」 머리 요약 `스위치 16 · 개폐기 8` 로 시험장비 설정과 대조 (2026-09-15) | ✅ `t_551` |
| 3 | 5.5.2 레벨1 스위치 제어 a)~j) | a) 5.5.1 뒤·스위치 포함, b) **화면**으로 작동시간 명령, c) 시험장비 수신·시간 동일, d) 시험장비 작동중, e) **화면에** 작동중 표시, f) **화면의** 남은시간 적절, g) 화면으로 중지, h) 수신, i) READY, j) **화면에** READY 표시 | b)g) 화면과 같은 입구 `POST /api/control/local`. e)f)j) 는 화면이 읽는 **NR 프록시 `/api/ks3267/status`** 로 판정 — f) 는 3회 샘플의 (경과, 표시, 실제) 가 폴링 2s+1.5s 안에서 맞고 감소하는지. 표준노드 탭 §5.1.3 표의 남은시간은 읽은 시각부터 **초 단위로 흐른다**(2026-09-15, 전엔 10초마다 멈춘 값). 노드 시험 §5.2.1(만료·재명령)은 `5.2.1` 로 분리 | ✅ `t_552` + 🟡 화면 캡처 |
| 4 | 5.5.3 레벨1 개폐기 제어 a)~s) | b)~j) 화면 작동시간 열기 → 수신·열림중 → **화면에** 열림중·남은시간 → 화면 중지 → READY 표시, k)~s) 닫기로 같은 순서 | §5.5.2 와 같은 화면 경로 판정(`_screen_phase` 공용): 열기 303 20s → 화면(NR 프록시) 301·남은시간 19/17/15 → 중지 → READY → 닫기 304 → 302·18/16/14 → 중지 → READY. 노드 시험 §5.2.2(만료·재명령 4단계)는 `5.2.2` 로 분리 (2026-09-15) | ✅ `t_553` + 🟡 화면 캡처 |
| 부가 | 스코프 선언 일치 — §5.3 비정상 명령의 제어기 쪽 대응 | (116 연동장비표 레벨2 ×) §5.3.1 b) 203·§5.3.2 305/306 은 **노드** 시험(시험장비가 노드에 보냄) — 레벨1 제어기는 그 코드를 만들지 않는다 | 드라이버가 스위치 203·개폐기 305/306 을 로컬 거부, 시험장비 명령 블록(cmd·OPID) 전후 동일 = 버스로 안 나감 (2026-09-15: 203 추가·항등식 판정 교체, `evidence-20260915-221420/` 6/6) | ✅ `t_extra_level2` |
| 참고 | §5.3.1 스위치 203 · §5.3.3 개폐기 305 비정상 명령 (노드 시험) | 노드에 203/305 → 작동 안 함·에러/READY·OPID → 0 → READY | 우리 드라이버가 **시험장비 마스터 역할**: `POST /command {op:203|305, test_unsupported:true}` 로 실제 버스에 송신(FC16 503 [203,OPID,0] / 567 [305,OPID,0]) → 노드 예외 0x03 → 시험장비 상태 READY·remain 0·적용 OPID 불변 → 화면 경로 0 → READY. `test_unsupported` 는 화면·NR 이 보내지 않는 시험 전용 키(2026-09-15), `evidence-20260915-221931/` 12/12 | ✅ `t_531`·`t_533` (`--only 5.3.1,5.3.3`) |
| 참고 | §5.3.4 개폐기 동일 OPID 명령 (노드 시험) | 같은 OPID 의 303/304/0 은 무시, OPID 를 새 값으로 바꾸면 활성화 — a)~y) | 드라이버가 시험장비 역할: `POST /command {…, test_opid: N}` 로 OPID 지정 송신, "OPID 를 새 값으로 변경" 은 `POST /test/opid` 가 **OPID 워드 1개만** FC16(568/572…) → 노드가 그 시점에 활성화(6.3.3). 화면 경로는 OPID 를 정할 수 없어 이 시험엔 안 쓴다. 단위시험 `test_same_opid_ignored_and_new_opid_activates`. `evidence-20260915-222408/` 25/25 | ✅ `t_534` (`--only 5.3.4`) |
| 116 | 연결단자 해제·재연결 (116/117 마. 1-가·2-가) | 노드 1/3 이상 해제 → 재연결 후 2분 이내 정상, 5회 | 시험장비 `/fault timeout`(버스 전 노드 = 100%) → `none`, 드라이버 `/status`·NR 프록시 복귀 시간 측정 | ✅ `t_116_reconnect` (`--only 116-재연결`) |

수동 캡처 절차 (5.5.2 e/f/j, 5.5.3 e/f/j/n/o/s): 하우스/센서 탭 → 장치 추가 `표준 시험 스위치`(스위치, 프로토콜 표준 노드 U1/스위치/1)·`표준 시험 개폐기`(측창, U1/개폐기/1) → 저장 → 제어판에서 ON(20초)/열기(15초) → 📐 배지 `켜짐 18s`/`열리는 중 12s` → 정지 → `READY` 캡처. 표준노드 탭 U1 카드의 해당 행(현재 상태·남은시간) 캡처. **시험 후 두 장치는 삭제**(운영 설정 원복).

## 3. KOAT 검정기준 116/117 「통합제어기」 대응표

### 3.1 규격 및 성능설명서 기입값 (초안)
- 형식: **복합형** (현장 키오스크 + 원격 웹/모바일). 관제 지원: 본체 패널(키오스크) ○ / 웹 ○ / Android·iOS PWA ○
- 관제 방식: 복합형 — 노드 ↔ 통합제어기 **RS-485 (KS X 3267 Modbus RTU 9600 8N1)**, 통합제어기 ↔ 클라우드 **MQTT(TLS)/HTTPS**
- 기체: OS Raspberry Pi OS 64bit, Processor Broadcom BCM2712 (RPi 5) / BCM2711 (RPi 4), Storage microSD(+NVMe 옵션), POWER 5V 5A 어댑터(+24VDC 노드 급전 PSU), Database SQLite(로컬)+PostgreSQL/TimescaleDB(서버)
- 연동장비표: 온도·습도·CO₂ — KS X 3267 ○ (센서 노드 디폴트맵 순번 1/4/13) · 레벨1 스위치 ○ · 레벨1 개폐기 ○ · **레벨0/2, 양액기 ×** · 그 외 센서는 표준 미적용(Waveshare/XY-MD02 자체 연동 ○ 표기)

### 3.2 검정기준 항목별 현황

| 항목 | 기준 | 현황 | 확인 방법 / 갭 |
|---|---|---|---|
| 센서 상태정보 연계 | 관제 방식별 이상 없음 | ✅ 표준노드 탭·제어판·키오스크 동일 백엔드 | 3개 관제 방식(패널/웹/앱)에서 같은 화면 반복 캡처 |
| 연결단자 해제·재연결 → 2분 내 정상 (5회) | 2분 | ✅ **자동 시험 `116-재연결` 17/17 통과 (2026-09-14)** — 5회 모두 해제 3~4초 만에 인지, 재연결 **3.0초** 만에 드라이버·화면 경로(NR 프록시) 복귀 | 증적 `evidence-20260914-102045/`. 3.0초는 진행 중이던 타임아웃 요청이 끝나는 시간 + 다음 폴링. 관제 방식별 화면 캡처와 실물 M12 물리 해제는 수동 |
| 데이터 손실률 ≤ 3 % (24h, 1440건 기준) | L=(1−N/1440)×100 | ✅ 1분 수집 + SQLite 오프라인 버퍼 + 서버 재전송 | 24h 후 `GET /api/sensors/farm_0001/house_0001/count?startDate=…&endDate=…` → N; 구동기는 `control_logs`/`device_positions` 건수 |
| 수집 주기 1분 설정 가능 | — | ✅ 설정 › 시스템 › 수집 주기(`collectionConfig.intervalSeconds`) | 60 으로 설정 캡처 |
| 30일 이상 데이터 확보 | 입고 시점 기준 | 🟡 **9/12 정전·9/13 농장 이동으로 창이 끊김** (표준 구동기 하루 손실 43.40%·47.57%, 9/6~11 은 0%) → **9/14 부터 다시 셈, 가장 빠른 입고 10/14** | 지표 `smartfarm_ks_data_window_days`·`smartfarm_ks_data_ready_in_days`, 어제 손실 3% 초과 시 경보 `KsDataWindowBroken` (2026-09-14 신설) |
| 시각화 1시간 이하 단위, 1/7/30일 | — | ✅ SensorChart 1h/6h/24h/7d/30d — 30일은 1시간 단위 720점 (2026-08-30 수정) | 캡처 |
| 조회 1분 단위, 1/7/30일 (3분 이내) | — | ✅ 보고서 › **데이터 조회·추출** 탭 (`DataExplorer.jsx`): 센서/제어 이력/표준 구동기 상태, 기간 1·7·30일·직접, 항목 선택, 1분 행 표, 소요 시간 표시 | 캡처 + 소요 시간 |
| 추출 .csv/.txt/.xls (3분 이내) | — | ✅ `.csv/.txt` 서버 추출(`GET /api/sensors/:farm/:house/export`, `/api/control-logs/:farm/export`, `/api/actuator-status/:farm/export`) + `.xlsx` 화면 생성. 결측은 빈 칸 | 파일 첨부 |
| 전원 차단 30분 → 복구 후 연계·저장 데이터 유지 | — | ✅ SQLite 영속, 드라이버 opid 파일 영속, pm2 자동 기동, 서버 DB 별도 | 실 전원 차단 시험 절차: RPi 전원 OFF 30분 → ON → 표준노드 탭 자동 복귀, 이전 데이터 조회 |
| 구동기 동작 제어 (정/OFF/역, ON/OFF) 관제 방식별 순차 | — | ✅ 제어판(웹/앱/키오스크) 동일 경로 | §5.5.2/5.5.3 자동 + 관제 방식 3종 캡처 |
| 구동기 상태정보 시각화·조회·추출 | 1시간 단위·1분 조회·추출 | ✅ `actuator_status` 테이블(1분 스냅샷: NR 「표준 구동기 1분 스냅샷」 → `/internal/actuator-status`, 실패 큐 재전송·멱등) + 조회·추출 화면. 드라이버 60초 하트비트 | ✅ NR 스냅샷 노드 적용 완료 (2026-08-30 C3 실증: 5단 파이프라인 관통·stale 억제 확인) |
| 조작 난이도 | 매뉴얼대로 용이 | ✅ `docs/ksx3267/manual/사용설명서.md` | 시험자가 설명서대로 조작 |
| 양액공급기 연계 | 연동 가능 제어기만 | — 해당 없음(연동장비표 ×) | 시험 제외 명시 |

### 3.3 116 을 위해 추가로 만든 것 (P7, 2026-08-30 완료)
1. CSV/TXT 추출 — `backend/src/utils/exportCsv.js`(순수, 테스트 12) + 라우트 3개. 31일 상한, 결측 빈 칸
2. 1분 단위 조회 화면 — `frontend/src/components/Dashboard/DataExplorer.jsx` (보고서 › 데이터 조회·추출)
3. 표준 구동기 상태 시계열 — `prisma/migration-actuator-status.sql`(하이퍼테이블), `/internal/actuator-status`(멱등), `/api/actuator-status`, NR `fn_ks_snapshot(.result).js`, 데몬 60초 하트비트
4. 30일 차트 1시간 단위(720점)
5. 사용설명서 `docs/ksx3267/manual/사용설명서.md`
운영 반영 순서: 서버 DB 에 `migration-actuator-status.sql` 적용 → 백엔드 배포 → NR 에서 `ks3267-snapshot-nodes.json` 가져오기(탭 열고) → Deploy → 제어판 매핑 장치 있으면 1분 뒤 `actuator_status` 행 확인

## 3.3.1 저장 정책 — 표준·비표준 동일 (2026-09-19)

| 대상 | 서버 (1분) | 서버 보관·압축 | 제어기 로컬 (1분) | 로컬 보관 |
|---|---|---|---|---|
| 센서 (비표준·매핑된 표준) | `sensor_data` | 삭제 없음 · 7일 뒤 압축 | Node-RED SQLite `sensor_data` | 서버 설정 retentionDays (기본 60) |
| 표준 센서 관측치·상태 | `ks_sensor_status` | 삭제 없음 · 7일 뒤 압축 | 드라이버 `sensor_minute` | 같은 retentionDays |
| 구동기 (표준) | `actuator_status` source=ks3267d | 삭제 없음 · 7일 뒤 압축 | 드라이버 `actuator_minute` | 같은 retentionDays |
| 구동기 (비표준 릴레이) | `actuator_status` source=vendor | 삭제 없음 · 7일 뒤 압축 | 드라이버 `vendor_actuator_minute` | 같은 retentionDays |

- 비표준 구동기 행은 NR 「응답 포맷」(릴레이 MQTT 상태 탭)이 30초마다 FC1 로 **실제로 읽은 코일**로 만든다(`global.vendorCoils`). 상태코드는 표준과 같게 켜짐 201 / 꺼짐 0 / 열림 코일 301 / 닫힘 코일 302. 3분 넘게 못 읽으면 행을 만들지 않는다(손실로 드러남). 남은시간·OPID 는 비표준에 없어 0.
- 압축 정책 `backend/prisma/migration-ks-compression.sql`(운영 DB 적용 2026-09-19, 재실행 안전). segmentby 가 PK 를 덮어 압축 청크에서도 재전송 멱등(`test/db/storage-policy.test.js`).
- 드라이버 로컬 보관 일수는 NR 「1분 스냅샷」 출력 2 가 매분 넘기는 `retentionDays` 를 따른다(7~3650일 밖은 무시).
- 남는 차이는 장치 능력뿐: 비표준 센서는 상태코드가 없어 `ks_sensor_status` 에 해당 행이 없다.

## 3.4 실노드 증적 — 화면 버튼 하나 (2026-09-19)

입고 시험장에는 인터넷도 SSH 도 없다. 자가시험 스크립트는 시험장비(시뮬레이터) API 로 값을 바꿔 가며 판정하므로 실 노드엔 못 쓴다. 그래서 표준노드 탭 **⑤ 「실노드 증적 만들기」** 를 두었다.

- 드라이버 `evidence.py` 가 "지금 읽히는 것" 만으로 판정한다: §5.4.1 a)~d)(`/conntest` 와 같은 판정) → §5.4.2/§5.5.1(노드정보 1~8·디바이스 코드·개수·종류) → 센서면 §5.4.3(마지막 폴링 관측치·상태코드 정의값, 변화 이력)·§5.4.4(로컬 SQLite 최근 10분 빈틈 없음) / 구동기면 §5.5.2·5.5.3(지금 상태·최근 명령 이력)·116 저장. 결과는 자가시험과 같은 3종 `report.md`·`results.json`·`frames.txt`(TX/RX 400개) 로 `~/smartfarm/ks3267/evidence/realnode-YYYYmmdd-HHMMSS/`.
- 경로: 패널 같은 출처 `/api/ks3267-comm/evidence` → rpi-server → 드라이버 `POST /evidence {unit}`(루프백). 웹은 `/config/:farmId/ks3267-evidence`(생성·삭제는 농장 소유자). 파일은 JSON `{content}` 로 받아 브라우저가 내려받기를 만든다.
- 판정이 통과하려면: 센서 노드는 등록 뒤 10분(§5.4.4) + 관측치 변화 1회 이상(§5.4.3), 구동기 노드는 제어판 명령 1회 이상(§5.5.2/5.5.3). 화면 캡처 항목은 report 의 「수동 증적 항목」에 체크리스트로.
- 첫 실노드 묶음: **`evidence-realnode-20260919-165013/` — 우노(ATmega328P+MAX485) 센서 노드 unit 1, 4/4 통과**. 노드 펌웨어는 `docs/stand_node/ksx3267_sensor_node_uno/`.
- 명령 이력은 드라이버 메모리가 아니라 **로컬 SQLite `command_log`**(api `/command`·`/test/opid` 마다 기록, 60일)에서 읽는다 — 재시작·정전 뒤에도 §5.5.2/5.5.3 명령 이력이 남는다(18:26 재시작으로 3/4 가 났던 사고). 시각은 KST 로 적는다.
- **명령 출처**: NR 「표준 명령 조립」이 `source {via, house, device, by}` 를 실어 보내고(사람 = `screen`, 자동화 operator `automation*`·`schedule_off_timer` = `auto`), 출처 없는 호출은 `direct`, `test_unsupported`·`test_opid`·`/test/opid` 는 `test`. 보고서 명령 이력 줄마다 `[화면 house_0003 heater1 by web_dashboard]` 처럼 표기.
- **§5.5.2·§5.5.3 은 따로 판정**하고 **화면 명령만** 쓴다: 스위치 = 202 수락(201·남은시간) → 남은시간 안에 화면 0 수락 → READY. 개폐기 = 303→301→중지→READY **와** 304→302→중지→READY 둘 다. 자연 만료 뒤의 OFF 는 "작동 중 중지" 가 아니다. 화면 명령 중 거부·무응답이 있으면 실패. 구동기 묶음은 5.4.1·5.5.1·5.5.2·5.5.3·116-저장 5개.
- **frames.txt**: 폴링 링버퍼(400) + 명령 프레임(FC06/16 요청과 그 응답, 따로 200개 보관)을 합쳐 시간순 — 폴링에 밀려 명령 프레임이 빠지던 문제(9/19 18:35 묶음은 최근 2분뿐).
- 단위시험 `test_evidence.py`(22개)·`test_localstore.py CommandLog`·`CommandLogOriginMigration`·`test_driver.py CommandOrigin`·`FrameLogWrites`.

## 4. 시험기관 사전 확인 (PLAN §4 그대로 + 문서 읽고 추가)
- SPS-7466 §5.4.1 b) "슬레이브 아이디" — 시험장비 노드 주소를 시험 당일 알려주는지, 우리는 표준노드 탭에서 입력 탐색
- §5.4.4 "제어기에서 제시한 저장주기" — 우리는 1분. 표준 센서를 기존 수집 파이프라인에 합류시키므로 저장주기 = 수집주기
- 116 연동장비표에서 "표준 미적용 장비 ○/×" — 자체 프로토콜 장비(Waveshare 릴레이, XY-MD02)를 어떻게 표기할지
- §5.5.2 c) "작동시간이 동일한지" — 초 단위 정수. 우리 UI 의 duration 은 초 → 그대로 전달 (반올림 없음) 확인됨

## §5.5.2 레벨 1 스위치 — b)~m) 완전판 (2026-09-04)
- (9/15 저녁부터 `5.5.2` 는 제어기 시험 a)~j) 화면 경로 판정이고, 아래 내용은 `--only 5.2.1` 로 분리된 노드 시험이다.)
- `selftest_sps7466.py --only 5.2.1` 가 §5.2.1 의 a)~m) 를 한 번에 수행한다: **a) 탐색 결과에서 해당 스위치 코드 102 확인**(없으면 해당 없음으로 종료, 2026-09-15 추가 — 5.5.3 도 같은 방식으로 개폐기 112 확인) → 202 TIMED_ON → 되읽기 OPID 동일·201 → 남은시간 감소 → **e) 노드 갱신 주기 직접 측정**(노드 레지스터 0.2s 샘플) → **f) 자연 만료 대기 → g) READY** → h)~j) 재명령(OPID 변경 확인) → k)~m) 0 OFF → READY.
- OPID 판정: 표준 g)/m) "OPID 동일" 과 표 16 "실행 중 명령 없으면 0" 이 겹치므로 READY + (명령 OPID 와 동일 **또는** 0) 을 통과로 하고 어느 쪽인지 detail 에 남긴다(시뮬레이터는 0). 실노드에서 다른 값이 나오면 그 자체가 판정 근거.
- 화면 재현: 제어판 표준 스위치 카드의 **「📐 시간 지정 ON」**(초 입력 → 202) → 📐 배지 '켜짐 NNs' 감소 → 'READY' 자동 복귀. 표준노드 탭 §5.1.3 표와 ④ 진단 프레임(FC16 503~506 / FC03 203~206)이 같은 사실을 보여준다.
- 증적: `evidence-20260904-151412/` (13/13 단계, 노드 갱신 주기 ≈1.0s, 제어기 표시 주기 2.0s).

## §5.5.3 레벨 1 개폐기 — b)~s) 완전판 (2026-09-04)
- (9/15 저녁부터 `5.5.3` 은 제어기 시험 a)~s) 화면 경로 판정이고, 아래 내용은 `--only 5.2.2` 로 분리된 노드 시험이다.)
- `selftest_sps7466.py --only 5.2.2`: **§5.2.2 a)~z) 네 단계 그대로** (2026-09-15 확장 — 9/4 판은 ③④ 만이라 개폐기의 만료 자동 중지 f)·m) 를 본 적이 없었다): a) 코드 112 확인 → ① 작동시간 열기(303) → 여는중(301)·감소·노드 갱신 주기 → **만료로 스스로 중지 → READY** → ② 작동시간 닫기(304) → 닫는중(302)·감소·주기 → **만료 → READY** → ③ 열기 → 여는중·감소 → 중지 명령(0) → READY → ④ 닫기 → 닫는중·감소 → 중지 → READY. 매 되읽기 OPID 동일, 매 명령 OPID 변경 확인. 증적 `evidence-20260915-221129/` (29/29, 노드 갱신 주기 ≈0.94~1.01s).
- 화면 재현: 제어판 표준 개폐기 카드 「📐 작동시간」 초 입력 → **⏱ 시간 열기 / ⏱ 시간 닫기**(303/304, 클릭 즉시 카운트다운) → 📐 배지 '열리는 중 NNs' / '닫히는 중 NNs' → **■ 정지**(0, 즉시 정지) → 'READY'. 일반 ▲열기/▼닫기는 301/302(노드의 완전 개폐 시간). 하우스2 `window1`(측창 1 → U1 개폐기1) 이 시험 장치.
- 끝단 증명: 화면과 동일한 AWS 페이로드(open, duration 12) → NR `timed_open (12초)` → 드라이버 `op 303, status 301, remain 12, accepted`.
